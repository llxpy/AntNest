# AntNest v1.3.1 Release Notes

> 版本：**v1.3.1** · 日期：2026-08-29
> 安装包：`AntNest-Setup.exe`（单用户安装到 `%LOCALAPPDATA%\AntNest`，无需管理员权限）
> GitHub Tag：`v1.3.1`

---

## 本次更新（v1.3.1）

### 🎨 UI 统一化修复

1. **输入行水平对齐**
   - 技能选择 / 模型选择 / 文本输入框 / 发送按钮，四者统一为同一个 **38px 水平面**（等高、垂直居中、同一基线）。
   - 修复前：select 默认内边距 + textarea 旧 padding 导致三者高低不一、视觉错位。

2. **文本输入框自动换行增高**
   - 输入框由 input 改为 **textarea**：输入超长自动换行，内容多时自动向上涨高（最高 120px），不再横向滚出。
   - **Enter = 发送，Shift+Enter = 换行**，可粘贴图片。

3. **白色主题精修**
   - 白色主题全面优化：纯白卡片、更实边框、正文/次要文字对比度拉满、输入与下拉白底 + 聚焦光晕、气泡/思维块/下拉箭头全部适配白底。
   - 黑色主题保持原有质感，两主题共用统一设计 token。

4. **下拉框（Skill / 模型）美化**
   - 统一样式：自定义箭头（SVG）、hover 高亮、聚焦光圈，不再使用系统默认丑下拉。

5. **工作空间（左侧栏）**
   - 可折叠：点 « 折叠为竖条，点竖条 / » 展开。
   - **文件预览**：点击工作空间列表项即在栏内预览该目录文件列表（目录优先），并可一键「进入此工作空间」。

### 🔴 关键修复回顾（v1.3.0→v1.3.1 之间）

- **停止按钮真正生效**：`agent_cancel` 双命名空间同步（模块级 + AntNest 壳），流式推理可被真正中断、工蚁进程树可被强杀。
- **关闭窗口 = 真退出**：移除 `_on_closing` 拦截与顶栏「退出」按钮，点 × 即干净退出，不再残留锁导致无法二次打开。
- **单实例锁自愈**：锁文件带时间戳，进程不可达 / 锁龄超过 6 小时 / 上次异常退出（startup_error.log 存在）时自动清锁放行。

### ✅ 既有能力（v1.3.x 已包含）

- 蚁后/工蚁架构：任务拆解、工蚁隔离执行（独立进程 + 临时目录）、失败自动重试一次。
- 深度思考全程可见：任务进行中流式显示推理过程（小卡片内滚动），正文随文本长度自适应。
- 记忆层：B+ 树思想索引 + RAG 检索（`memory_tree.py`），每轮检索 NEST.md / hints.md 注入上下文。
- 自我反思策略：连续 3 次相同工具+同参数 → 注入《自我反思》换方法继续，不再卡死中断。
- 对话记录：只读「查看」历史（含思维过程）、「恢复上下文」、删除可用；启动不再自动载入全部历史（省 token）。
- 模型能力画像：按 `/models` 实测校准 TOKEN_CAP 并注入系统提示词。

---

## 安装包核对（上传前自查）

| 文件 | 大小 | SHA-256 |
|---|---|---|
| `AntNest-Setup.exe` | 6,208,026 字节 (5.92 MB) | `29ce60c00db8ea88213672c167af1e161ec0392f2ff82730ed4b1cca03848170` |

> 注：安装包于 2026-08-29 14:55 编译，版本号 `1.3.1`。
> 上传后可用 `Get-FileHash .\AntNest-Setup.exe -Algorithm SHA256` 校验。

---

## 测试验证

- 全量回归：**82 passed + 17 subtests**
- `py_compile` 全核心模块通过
- `node --check` JS 语法通过
- 输入行等高 / 白色主题 / 折叠 / 预览 DOM 渲染冒烟通过

---

## 致谢与定位

**AntNest（蚁巢）**——本地运行 · 蚁后/工蚁架构 · 深度思考实时可见 · 全程掌控本机。
你的 AI，你的电脑，你的规则。